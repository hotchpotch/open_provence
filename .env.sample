# Environment variables for Open Provence tooling
# Copy to .env and replace values with your own secrets.

# Required when using MLDR LLM-based evaluation or the Streamlit WebUI "LLM judge" features.
OPENAI_API_KEY=sk-xxxxxxxxxx
